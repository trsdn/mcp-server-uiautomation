# UIAutomationMcp Skills

This repository currently keeps two lightweight skill entry points for UI Automation workflows:

- `uia-cli` - CLI-oriented guidance
- `uia-mcp` - MCP-oriented guidance

Both are intentionally small and aligned with the live UI Automation surfaces in this repo.

